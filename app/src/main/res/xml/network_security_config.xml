<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted only for local development super nodes (emulator host loopback and
  common LAN ranges); all other traffic remains HTTPS-only. Remove/restrict for production once a
  TLS Director endpoint exists (D-2 / M1-4 TLS pinning).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.8.80</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
