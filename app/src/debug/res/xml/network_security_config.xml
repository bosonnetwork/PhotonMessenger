<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Cleartext HTTP is permitted for local development super nodes (emulator host loopback
  and the dev LAN host) so the app can reach an http:// dev Director. All other traffic stays
  HTTPS-only. This file exists only in the debug source set, so it can never ship in a release APK;
  the release/production policy is the strict main config (HTTPS-only). D-2 / X-S4.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.8.80</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
